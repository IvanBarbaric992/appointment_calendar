import { Typography } from '@material-ui/core';

import './Footer.scss';

const Footer = () => (
  <footer className="footer--container">
    <Typography variant="h5">Interesting calendar</Typography>
  </footer>
);

export default Footer;
